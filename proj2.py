#use variables to store user inputs

first_Name = (input("What's your first name? "))
last_Name = (input("What's your last name? "))
id_no = (input("What is your age? "))
employment = (input("What is your employment status? "))

print(first_Name, last_Name, id_no, employment)
